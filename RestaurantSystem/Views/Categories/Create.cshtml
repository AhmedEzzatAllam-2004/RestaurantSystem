@model RestaurantSystem.Models.Category

@{
    ViewData["Title"] = "New Category";
}

<style>
    body {
        background-color: #fdfbfb;
    }

    .form-card {
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        border: none;
        overflow: hidden;
    }

    .form-header {
        background: linear-gradient(120deg, #e0c3fc 0%, #8ec5fc 100%); /* بنفسجي في أزرق فاتح */
        padding: 25px;
        color: white;
        text-align: center;
    }

    .form-floating > label {
        left: 10px;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card form-card">
                <div class="form-header">
                    <h3 class="fw-bold mb-0"><i class="fas fa-tags me-2"></i>New Category</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="form-floating mb-4">
                            <input asp-for="Name" class="form-control rounded-pill px-4" placeholder="Category Name" />
                            <label asp-for="Name" class="px-4"><i class="fas fa-heading me-2 text-primary"></i>Category Name</label>
                            <span asp-validation-for="Name" class="text-danger small ms-3"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary rounded-pill py-2 fw-bold shadow-sm" style="background: #8ec5fc; border:none;">
                                <i class="fas fa-plus me-2"></i> Add Category
                            </button>
                            <a asp-action="Index" class="btn btn-light rounded-pill py-2 border">Back</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
 }