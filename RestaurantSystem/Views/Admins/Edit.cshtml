@model RestaurantSystem.Models.Admin

@{
    ViewData["Title"] = "Edit Admin";
}

<style>
    body {
        background-color: #f0f2f5;
    }

    .form-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        border: none;
        overflow: hidden;
    }

    .form-header {
        background: #34495e; /* درجة أفتح قليلاً */
        padding: 25px;
        color: white;
        text-align: center;
    }

    .form-floating > label {
        left: 10px;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card form-card">
                <div class="form-header">
                    <h3 class="fw-bold mb-0"><i class="fas fa-user-cog me-2"></i>Update Admin</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="AdminId" />

                        <div class="form-floating mb-3">
                            <input asp-for="Username" class="form-control rounded-pill px-4" placeholder="Username" />
                            <label asp-for="Username" class="px-4"><i class="fas fa-user me-2"></i>Username</label>
                            <span asp-validation-for="Username" class="text-danger small ms-3"></span>
                        </div>

                        <div class="form-floating mb-4">
                            <input asp-for="Password" class="form-control rounded-pill px-4" placeholder="Password" />
                            <label asp-for="Password" class="px-4"><i class="fas fa-key me-2"></i>Password</label>
                            <span asp-validation-for="Password" class="text-danger small ms-3"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-secondary rounded-pill py-2 fw-bold shadow-sm">
                                <i class="fas fa-save me-2"></i> Save Updates
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill py-2">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
 }