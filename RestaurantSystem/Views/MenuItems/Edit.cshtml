@model RestaurantSystem.Models.MenuItem

@{
    ViewData["Title"] = "Edit Menu Item";
}

<style>
    body {
        background-color: #f8f9fa;
    }

    .form-card {
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        border: none;
        overflow: hidden;
    }

    .form-header {
        background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%); /* لون أزرق سماوي للتعديل */
        padding: 30px;
        color: #333;
        text-align: center;
    }

    .form-floating > label {
        left: 10px;
    }

    .form-select {
        border-radius: 50px;
        padding-left: 1.5rem;
        height: 58px;
    }
</style>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">

            <div class="card form-card">
                <div class="form-header">
                    <h2 class="fw-bold mb-0"><i class="fas fa-edit me-2"></i>Edit Dish</h2>
                    <p class="mb-0 opacity-75">Update details for @Model.Name</p>
                </div>

                <div class="card-body p-4 p-md-5">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="ItemId" />

                        <div class="form-floating mb-3">
                            <input asp-for="Name" class="form-control rounded-pill px-4" placeholder="Item Name" />
                            <label asp-for="Name" class="px-4"><i class="fas fa-tag me-2"></i>Dish Name</label>
                            <span asp-validation-for="Name" class="text-danger small ms-3"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="Price" class="form-control rounded-pill px-4" placeholder="Price" type="number" step="0.01" />
                            <label asp-for="Price" class="px-4"><i class="fas fa-dollar-sign me-2"></i>Price</label>
                            <span asp-validation-for="Price" class="text-danger small ms-3"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <select asp-for="BranchId" class="form-select px-4" asp-items="ViewBag.BranchId"></select>
                            <label asp-for="BranchId" class="px-4"><i class="fas fa-store-alt me-2"></i>Branch</label>
                            <span asp-validation-for="BranchId" class="text-danger small ms-3"></span>
                        </div>

                        <div class="form-floating mb-4">
                            <select asp-for="CategoryId" class="form-select px-4" asp-items="ViewBag.CategoryId"></select>
                            <label asp-for="CategoryId" class="px-4"><i class="fas fa-list me-2"></i>Category</label>
                            <span asp-validation-for="CategoryId" class="text-danger small ms-3"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-info text-white rounded-pill py-3 fw-bold shadow-sm">
                                <i class="fas fa-save me-2"></i> Save Changes
                            </button>
                            <a asp-action="Index" class="btn btn-light rounded-pill py-3 fw-bold text-muted border">
                                <i class="fas fa-times me-2"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}